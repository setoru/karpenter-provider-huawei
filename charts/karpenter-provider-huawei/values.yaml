# Default values for karpenter-provider-huawei

# -- Number of controller replicas
replicaCount: 1

# Controller image configuration
image:
  # -- Controller image repository
  repository: swr.ap-southeast-3.myhuaweicloud.com/hwclouddeveloper/cce/karpenter/controller
  # -- Controller image tag
  tag: "0.3.2"
  # -- Image pull policy
  pullPolicy: IfNotPresent

# -- Image pull secrets for controller pods
imagePullSecrets: []
# - name: registry-credentials

# -- Name prefix for all resources
namePrefix: "karpenter-provider-huawei-"

serviceAccount:
  # -- Create ServiceAccount
  create: true
  # -- ServiceAccount name
  name: controller-manager

# CCE Pod Identity integration.
# When enabled, the controller Pod is labeled so CCE can inject
# HC_CONTAINER_AUTHORIZATION_TOKEN_FILE and HC_CONTAINER_CREDENTIALS_FULL_URI.
# The ServiceAccount must still be associated with an IAM agency in CCE.
podIdentity:
  # -- Enable CCE Pod Identity injection for the controller Pod
  enabled: false

service:
  # -- Additional annotations for the metrics Service
  annotations: {}

serviceMonitor:
  # -- Specifies whether a ServiceMonitor should be created
  enabled: false
  # -- Additional labels for the ServiceMonitor
  additionalLabels: {}
  # -- Relabelings for the `http-metrics` endpoint on the ServiceMonitor
  relabelings: []
  # -- Metric relabelings for the `http-metrics` endpoint on the ServiceMonitor
  metricRelabelings: []
  # -- Additional configuration on the `http-metrics` endpoint
  endpointConfig: {}
  # -- Per-scrape limit on the number of scraped samples accepted by Prometheus
  sampleLimit: null

# Controller arguments
controller:
  metrics:
    # -- Metrics port
    port: 8080
  healthProbe:
    # -- Health probe port
    port: 8081

# Huawei Cloud credentials
# Leave accessKey and secretKey empty when podIdentity.enabled is true.
credentials:
  # -- Create a Secret for credentials
  create: true
  # -- Secret name for Huawei Cloud credentials
  name: "huawei-credentials"
  # -- Use an existing Secret when credentials.create is false
  existingSecret: ""
  # -- Huawei Cloud access key
  accessKey: ""
  # -- Huawei Cloud secret key
  secretKey: ""
  # -- Huawei Cloud region ID
  region: ""
  # -- Optional Huawei Cloud project ID override
  projectId: ""
  # -- Optional BSS endpoint override for non-China websites
  bssEndpoint: ""

clusterInfo:
  # -- Huawei Cloud CCE cluster ID
  clusterID: ""
  # -- Cluster category: Optional. Enter "eni" for Turbo network types.
  # For other network types (vpc-router or overlay_l2), enter other values.
  category: ""

# -- yangtseEipInfo: Supports user-defined EIPs (Elastic IPs) bound to Karpenter pods.
# This only takes effect when clusterInfo.category is set to "eni".
yangtseEipInfo:
  yangtse.io/pod-with-eip: "true"

# Controller resources
resources:
  limits:
    cpu: "1"
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi

# -- Pod security context
podSecurityContext:
  runAsNonRoot: true
  seccompProfile:
    type: RuntimeDefault

# -- Container security context for manager
securityContext:
  readOnlyRootFilesystem: true
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - "ALL"

# -- Node selector for controller pods
nodeSelector: {}

# -- Tolerations for controller pods
tolerations: []

# -- Affinity for controller pods
affinity: {}
